# AGENTS.md

本文件约束整个仓库内的代码设计、实现、测试、文档和维护行为。所有新增代码、重构和缺陷修复都必须遵守本文件；如果子目录存在更具体的 `AGENTS.md`，则子目录规则可以补充本文件，但不得降低本文件规定的工程质量。

## 1. 工程目标

- 本项目按可长期维护、可测试、可观测、可演进的生产级工程建设，不以“先跑起来”为理由提交玩具式实现。
- 优先解决真实业务问题。不得为了展示设计模式、增加代码量或快速拼凑功能而引入无实际用途的类、接口、配置、目录和兼容层。
- 参考传统 Web 应用的分层思想组织代码，明确 Controller、Service、Repository/DB、Domain、CLI 和基础设施的职责。
- 保持前端、HTTP API、Service、数据模型、CLI 的概念名称和行为一致。同一业务概念不得在不同层使用无理由的不同命名。
- 优先采用简单、直接、可读的控制流，避免层层嵌套、过长函数、隐式副作用和跨层调用。
- 修改范围应与需求匹配。不要顺手重写无关模块，不要通过大规模格式化掩盖实际改动。

## 2. 推荐目录结构

现有代码允许渐进式迁移，不要求仅为满足目录形式而一次性搬迁。新增业务模块应优先遵循以下结构：

```text
application/
  controllers/          # HTTP/SSE 请求接入、参数校验、响应转换
  services/             # 用例编排、事务边界、业务流程
  repositories/         # 数据访问接口与实现
  db/                   # 数据库连接、会话、迁移、持久化模型
  domain/               # 领域实体、值对象、领域规则
  schemas/              # API/CLI 输入输出模型和跨层 DTO
  agent/                # 模型调用、Agent 编排及会话执行
  prompts/              # 系统、意图识别和任务提示词
  tools/                # 工具协议、注册、权限与工具实现
  cli/                  # CLI 命令解析、命令处理入口
  infrastructure/       # 外部 API、缓存、消息、文件系统等适配器
  static/               # 前端静态资源
  config.py             # 集中配置读取与校验
  web.py                # Web 应用装配与启动入口
  __main__.py           # python -m 入口
tests/
  unit/
  integration/
  e2e/
docs/
```

只有实际存在对应职责时才创建目录或文件。禁止添加空实现、占位 Service、无调用 Repository、仅转发一次的包装类，以及没有近期使用场景的“未来扩展点”。

### 2.1 现有模块的职责映射

- `application.app` 是应用装配入口，只负责创建应用、注册路由和装配依赖。
- `application.controllers` 是 HTTP/SSE Controller 层，不得加入模型或持久化逻辑。
- `application.services` 是应用服务层，负责对话、工作流、写工具与评估用例。
- `application.repositories` 是会话和知识数据访问层，不得依赖 Controller。
- `application.domain` 保存领域模型和规则，不依赖 FastAPI、CLI 或具体数据库连接。
- `application.agent` 和 `application.tools` 分别是模型执行与工具边界，业务写入必须调用 Service。
- `application.prompts` 集中保存提示词常量，运行时不得继续内嵌大段提示词文本。
- `examples/legacy` 只保留历史实验，不得被正式运行链路依赖。

## 3. 分层职责与调用方向

推荐调用方向：

```text
Web Controller / CLI
        -> Service
        -> Domain
        -> Repository interface
        -> Repository / DB / Infrastructure implementation
```

### 3.1 Controller 层

- 只负责协议相关工作：接收请求、参数校验、身份上下文提取、调用 Service、转换响应和映射错误。
- 不得直接执行 SQL、读写数据库、调用底层文件系统或实现核心业务规则。
- 不得在路由函数中编写复杂流程。出现多个业务步骤、条件分支或复用需求时，应下沉到 Service。
- HTTP 状态码、SSE 事件名、错误响应格式必须保持统一。
- Pydantic 请求和响应模型应放在 `schemas/` 或对应 Controller 模块的清晰位置，避免散落的匿名字典。

### 3.2 Service 层

- 每个 Service 对应明确的业务能力或用例，负责流程编排、权限判断、事务边界和跨 Repository 协作。
- Service 不依赖 FastAPI 请求对象、CLI 参数解析器或具体数据库连接。
- Service 返回明确的领域对象、DTO 或结果类型，不返回难以约束的多层匿名字典。
- 可复用业务规则只能保留一份。Controller、CLI、Tool 不得各自实现一套相似规则。
- Service 之间避免双向依赖。需要共享的稳定规则应提取到 Domain 或专用组件，而不是互相调用形成环。

### 3.3 Repository 与 DB 层

- Repository 封装数据访问语义，例如 `get_session_by_id`、`save_ticket`，不得向上层泄漏 SQL、游标或 ORM 查询细节。
- Repository 接口按业务需求设计，不创建通用但语义模糊的 `BaseRepository` 或万能 CRUD。
- `db/` 负责连接、会话、事务、迁移和持久化模型；事务由 Service 用例边界控制或通过明确的 Unit of Work 管理。
- 数据库查询要考虑索引、分页、批量访问和 N+1 问题。禁止先全量加载再由 Python 过滤可由数据库完成的数据。
- 写操作必须明确失败行为、一致性要求和幂等策略。迁移必须可追踪、可回滚或提供明确恢复方案。
- 测试不得依赖开发者本机数据库状态。

### 3.4 Domain 层

- 领域对象表达业务状态和规则，不依赖 Web 框架、CLI 框架、数据库 ORM 会话或第三方 API Client。
- 对有业务含义的状态优先使用枚举、值对象或明确类型，不使用散落的魔法字符串和数字。
- 领域异常必须语义清楚，例如 `SessionNotFoundError`，避免统一抛出 `Exception` 或 `RuntimeError`。

### 3.5 Infrastructure 层

- 外部模型、搜索服务、文件系统、缓存、消息系统等调用必须通过边界适配器封装。
- 配置统一从 `config.py` 注入，业务代码不得到处读取环境变量。
- 外部调用必须设置超时，并根据业务需要处理重试、限流、幂等、日志脱敏和可降级行为。
- 不得把 API Key、Token、Cookie、密码或完整敏感响应写入日志、测试快照和错误消息。

## 4. CLI 设计

- CLI 是与 Web Controller 并列的输入适配层，必须复用相同的 Service，不能建立独立业务实现。
- CLI 文件放入 `application/cli/`；`__main__.py` 和 `pyproject.toml` 中的命令入口只做启动和命令注册。
- 按业务动作设计子命令，例如 `serve`、`evaluate`、`session reset`，避免把所有参数堆进单个入口函数。
- 参数名、默认值、枚举范围和错误语义应尽量与 HTTP API 保持一致。
- CLI 应提供准确的 `--help`、合理退出码和面向用户的错误信息；调试堆栈只在显式调试模式输出。
- CLI 输出与日志分离。机器消费场景提供稳定的 JSON 输出，禁止在 JSON 中混入进度文本。
- 需要新增 CLI 时，同时更新 `pyproject.toml` 和对应测试；README 只在用户明确决定推出新版本或明确要求修改时更新。不需要 CLI 的内部能力不得强行暴露命令。

## 5. 前后端与接口一致性

- 前端字段名、API Schema、Service DTO 和 CLI JSON 字段应使用同一业务术语。
- API 路径、SSE 事件类型、状态枚举和错误码由后端定义为稳定契约，前端不得依赖临时文本解析业务状态。
- 修改接口时必须同步检查 `application/static/`、测试和示例。README 由产品版本发布节奏管理，只有用户明确决定推出新版本或明确要求修改 README 时才允许更新。
- 新增或删除模型字段时，应明确兼容策略。不得静默改变字段含义或复用旧字段表达新概念。
- 时间、时区、金额、百分比、ID 和枚举必须有统一格式；不可让前端猜测单位或状态。
- 前端只负责展示和交互，不得复制权限、幂等、敏感信息过滤等服务端规则。

## 6. 代码组织与控制流

- 优先使用早返回、守卫语句和小型辅助函数降低嵌套层级。
- 正常业务函数的控制流嵌套原则上不超过 3 层。超过时应重新审视职责、提取步骤或改用清晰的映射/策略。
- 不使用无意义的 `else`；前置条件失败后直接返回或抛出语义明确的异常。
- 单个函数只完成一项可描述的工作。无法用一句话准确说明时，通常应拆分。
- 避免超长参数列表。参数属于同一业务输入时使用明确的请求模型或值对象，但不得为了减少参数数量创建无语义容器。
- 禁止使用布尔参数制造隐蔽的双重行为，例如 `handle(data, is_special=True)`；应使用两个清晰命名的入口或策略对象。
- 不允许跨层“快捷调用”，例如 Controller 直接访问 Repository、Tool 直接执行 SQL、Repository 调用 Web 层。
- 不捕获异常后静默忽略。处理不了的异常应继续抛出，能处理的异常应记录必要上下文并转换为明确结果。
- 不保留注释掉的旧代码、调试输出、临时分支和没有任务关联的 TODO。

推荐：

```python
def load_session(session_id: str) -> Session:
    """根据会话 ID 读取会话，不存在时抛出明确的业务异常。"""
    if not session_id:
        raise InvalidSessionIdError("会话 ID 不能为空")

    session = session_repository.get_by_id(session_id)
    if session is None:
        raise SessionNotFoundError(session_id)

    return session
```

避免：

```python
def handle(data, flag=False):
    if data:
        if flag:
            try:
                # 多层嵌套并混合多个职责
                ...
            except Exception:
                pass
```

## 7. 命名规范

- 函数和方法名必须体现动作与对象，优先使用 `create_ticket`、`load_session`、`validate_model_name` 这类可直接理解的名称。
- 禁止使用 `do_it`、`process_data`、`handle_stuff`、`func1`、`tmp` 等缺少业务含义的命名。
- 布尔值使用 `is_`、`has_`、`can_`、`should_` 等前缀，使真假语义明确。
- 集合使用复数名，映射名称应体现键和值的意义。
- 类名使用明确职责名；只有确实承担服务、仓储或适配职责时才使用 `Service`、`Repository`、`Adapter` 后缀。
- 文件名与其主要职责一致。禁止使用持续膨胀的 `utils.py`、`common.py`、`helpers.py` 存放无关函数。
- 同一概念全项目统一命名。例如确定使用 `session_id` 后，不再混用 `conversation_id`、`chat_id`，除非它们确实是不同概念。

## 8. 中文注释与文档字符串

- 每个新增或修改的函数、方法、异步函数和生成器都必须提供中文文档字符串，准确说明它完成的工作。
- 文档字符串应描述职责、关键约束和重要副作用，不要逐字复述函数名。
- 对外接口、复杂算法、权限校验、事务、并发、重试和安全边界，应补充参数、返回值、异常或设计原因。
- Lambda 应仅用于一眼可懂的简单表达式；复杂逻辑必须改为带中文文档字符串的命名函数。
- 代码内行注释用于解释“为什么”，不得用注释重复显而易见的“做什么”。
- 修改缺少文档字符串的既有函数时，应在本次修改涉及的范围内补齐，不要求无关改动一次性覆盖整个仓库。
- 测试函数同样需要中文文档字符串，说明被验证的行为和预期结果。

示例：

```python
def build_tool_registry(settings: DeepSeekSettings) -> ToolRegistry:
    """根据运行配置注册可用工具，并关闭未配置依赖的外部能力。"""
    ...
```

## 9. 类型、模型与契约

- 所有新增 Python 函数都必须声明参数和返回值类型，包括内部辅助函数。
- 优先使用明确的 Pydantic Model、dataclass、TypedDict、Protocol 或领域类型表达数据结构。
- `Any` 只能出现在第三方库边界、动态 JSON 或确实无法静态表达的场景，并应尽快在边界内转换为明确类型。
- 不使用可变对象作为默认参数。
- 公共函数的返回结构必须稳定。不要根据分支返回互不相关的类型，也不要依赖调用方猜测字典中是否存在关键字段。
- JSON、SSE 和工具调用参数必须经过 Schema 校验，不得信任模型、浏览器或外部服务输入。

## 10. 日志、错误与可观测性

- 使用结构化日志记录请求 ID、会话 ID、阶段、耗时和结果状态；敏感字段必须脱敏。
- 禁止使用 `print` 作为正式日志方案。CLI 的用户输出除外。
- 错误分为领域错误、输入错误、依赖错误和系统错误，并在 Controller/CLI 边界统一转换。
- 面向用户的错误信息应可理解且不泄漏堆栈、密钥、内部路径或第三方敏感响应。
- 可以在用户明确启用深度思考时，向当前用户实时展示 DeepSeek 官方 API 通过 `reasoning_content` 字段返回的模型思考内容；必须明确标注这是模型生成的思考过程，不得将其表述为系统事实。
- 不得暴露 Coding-Harness、Codex 或其他内部组件未通过公开响应字段提供的隐藏推理。DeepSeek `reasoning_content` 可以传给当前请求的前端，但日志、Trace 和持久化审计事件只保存字符计数，不保存推理原文。
- 对外部调用、关键工具执行和持久化写操作保留足够的可追踪信息。

## 11. 安全要求

- 所有外部输入均视为不可信，包括 HTTP、CLI、模型输出、检索内容、工具参数和文件路径。
- 文件访问必须做工作区边界检查、规范化和敏感文件过滤。
- 命令执行必须使用参数列表和白名单，不得通过 Shell 拼接用户输入。
- 写操作需要权限校验；高风险或不可逆操作还需要明确确认和幂等保护。
- 数据库查询必须参数化，不得拼接 SQL。
- 新依赖应说明用途并评估维护状态、许可证和安全风险。不得仅为一个简单函数引入大型依赖。

## 12. 测试要求

- 新功能和缺陷修复必须附带能证明行为的测试。
- Service 和 Domain 规则以单元测试为主；Controller、Repository、外部适配器使用集成测试；关键用户流程可增加端到端测试。
- 测试名称应清楚表达条件和预期，例如 `test_confirmed_ticket_creation_is_idempotent`。
- 测试关注可观察行为，不绑定无关实现细节。
- 外部模型、搜索、时间、随机数和网络请求必须通过可替换依赖进行隔离，默认测试不得消耗真实 Token 或依赖公网。
- 修复缺陷时先添加能够复现问题的回归测试，再完成修复。
- 涉及并发、事务、缓存、权限、重试、幂等和安全过滤的改动，必须覆盖失败路径和边界条件。

提交前至少运行：

```bash
python -m unittest discover -s tests -v
ruff check .
```

如果项目后续引入格式化、类型检查或其他测试命令，应以 `pyproject.toml` 为日常维护来源，不得在不同文档中保留互相冲突的命令；README 仅在用户明确决定推出新版本或明确要求修改时同步。

## 13. 新增文件与依赖的判断标准

新增文件前必须确认至少满足一项：

- 承载独立且稳定的业务职责；
- 隔离明确的外部依赖或协议边界；
- 降低现有模块已经出现的真实复杂度；
- 支持可复用且已有两个以上调用场景的能力；
- 是运行、测试、迁移、配置或文档所必需的文件。

不得因为以下原因新增文件或代码：

- 猜测未来可能会用到；
- 单纯追求目录“完整”；
- 为一个简单调用增加接口、抽象类、工厂和实现类的完整套件；
- 复制已有逻辑以快速完成新入口；
- 添加永远返回固定值的占位实现；
- 用兼容层掩盖可以直接修正的内部调用。

引入依赖前优先检查标准库和现有依赖能否完成任务。新增运行时依赖必须同步更新 `pyproject.toml`、依赖说明、配置示例和相关测试。

## 14. 变更流程

1. 先阅读需求涉及的入口、调用链、数据模型、测试和文档。
2. 明确改动属于 Controller、Service、Domain、Repository、DB、CLI 还是 Infrastructure。
3. 选择满足需求的最小完整方案，确认没有复制业务规则或破坏层级方向。
4. 先更新或新增测试，再实现代码；小型纯文档修改可直接修改。
5. 修改接口时同步更新前端、Schema、CLI、示例和文档。
6. 每次发布新版本时，必须在 `docs/releases/` 新增一份版本更新文本，文件名使用版本号，例如 `v1.2.0.md`。文本至少说明版本号、发布日期、用户可感知的新增/变更、修复项、兼容性或迁移影响，以及已知限制；无对应内容时应明确写“无”。
7. 运行相关测试、全量测试和静态检查，记录未能执行的验证项。
8. 检查是否引入了无用代码、过度抽象、深层嵌套、模糊命名、未注释函数或敏感信息。

## 15. 完成标准

一个任务只有同时满足以下条件才算完成：

- 功能行为符合需求，失败路径有明确处理；
- 分层职责清楚，没有新增反向依赖或重复业务逻辑；
- 新增和修改的函数具有中文文档字符串、完整类型和友好命名；
- 前端、API、Service、CLI 和文档中的契约保持一致；
- 没有为凑结构添加无效代码、空目录和无调用抽象；
- 相关测试已补充并通过，静态检查无新增问题；
- 配置、安全、日志和兼容性影响已被检查；
- 架构文档或示例在行为变化时已同步更新；README 仅在用户明确决定推出新版本或明确要求修改时同步。
- 如涉及版本发布，已新增对应版本的更新文本，且内容与实际变更一致。
