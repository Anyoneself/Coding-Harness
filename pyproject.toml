[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "coding-harness"
version = "0.1.0"
description = "Recoverable coding harness with controlled tools and a Web console"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "openai>=1.55.0",
  "psycopg[binary,pool]>=3.2.0,<4.0.0",
  "python-dotenv>=1.0.1",
  "uvicorn[standard]>=0.30.0",
]

[project.optional-dependencies]
dev = [
  "httpx>=0.28.0",
  "ruff>=0.12.0",
]

[project.scripts]
coding-harness = "application.cli:main"

[tool.setuptools.packages.find]
include = ["application*"]

[tool.setuptools.package-data]
application = ["static/*", "db/migrations/*.sql"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
ignore = ["E501"]
select = ["E", "F", "I", "UP"]
