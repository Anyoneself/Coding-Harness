# language: zh-CN
功能: Coding-Harness 执行工作台
  为了在浏览器中可靠观察和控制持续代码任务
  作为本机开发者
  我希望界面在刷新、流式执行和不同屏幕尺寸下保持一致状态

  场景: 可恢复的本地 Thread 历史
    假如 浏览器保存了结构合法的最近任务
    当 用户重新打开工作台
    那么 最近任务应按更新时间展示
    并且 不合法或超出数量限制的数据不应破坏页面

  场景: 按序消费公开 Turn 事件
    假如 一个 Turn 正在持续返回公开事件
    当 客户端收到重复、倒序和分块到达的 SSE 数据
    那么 界面只应按递增序号归约事件
    并且 最终回答和 Turn 终态应保持一致

  场景: 首次运行安全配置
    假如 服务尚未配置模型密钥
    当 用户提交格式合法的 API Key
    那么 界面应等待服务确认后进入就绪状态
    并且 API Key 不应写入浏览器持久化

  场景: 响应式执行控制台
    假如 用户正在查看同一个 Turn
    当 视口从桌面宽度变为移动宽度
    那么 对话、输入和运行控制仍应可用
    并且 Thread 导航与事件检查器应通过独立抽屉访问

  场景: 浅蓝流光品牌空状态
    假如 当前 Thread 尚无公开消息且模型已经就绪
    当 用户打开 Coding-Harness 工作台
    那么 页面应显示当前项目、当前模型和任务导向主标题
    并且 同一个任务输入器应位于首屏任务发起区

  场景: 顶部导航只映射真实执行视图
    假如 工作台已经显示现有 Turn 检查器
    当 用户选择执行事件或 Turn 顶部导航
    那么 检查器应选择对应的现有标签
    并且 页面不应展示尚未支持的评估或工具入口

  场景: 空状态自然进入执行布局
    假如 用户正在浅蓝流光空状态中输入任务
    当 第一条公开用户消息进入当前 Thread
    那么 对话应继续显示在同一工作台框架
    并且 同一个任务输入器应回到执行区域底部

  场景: 移动端独立访问两侧信息
    假如 用户在 390×844 视口打开工作台
    当 用户依次打开和关闭任务导航与 Turn 检查器
    那么 两个抽屉都应具有明确的可访问控制
    并且 中央任务输入流程不应产生横向溢出或遮挡

  场景: 桌面默认使用完整任务画布
    假如 用户在桌面视口打开一个空 Thread
    当 用户尚未选择执行事件或 Turn
    那么 检查器不应占据固定布局宽度
    并且 标题与宽玻璃输入器应在完整主画布中形成稳定上下层级

  场景: 执行检查器按需覆盖主画布
    假如 桌面主画布已经完整显示
    当 用户打开并关闭执行事件检查器
    那么 检查器应以覆盖式抽屉出现和离开
    并且 主画布与任务输入器宽度不应发生永久压缩

  场景: 执行态延续流光视觉
    假如 当前 Thread 已经包含公开消息
    当 用户继续查看对话或输入下一项任务
    那么 流光背景应保持可辨认且正文对比度清晰
    并且 消息与输入器应使用隐藏检查器后释放的阅读宽度

  场景: 空状态只显示真实工程上下文
    假如 公开配置包含当前 Workspace 与只读模型名称
    当 用户查看空任务输入器
    那么 项目和模型应使用公开配置中的真实值
    并且 页面不应伪造分支、模型选择或多项目入口

  场景: 未配置与配置读取失败具有不同恢复路径
    假如 模型尚未配置或公开配置无法读取
    当 用户尝试发起首条任务
    那么 未配置状态应进入现有 API Key 安全配置流程
    并且 配置读取失败状态不应表现为发送成功

  场景: 输入器保留键盘提交契约
    假如 模型已经就绪且任务输入器获得焦点
    当 用户使用 Shift+Enter 换行并使用 Enter 提交
    那么 换行应保留在任务文本中
    并且 系统只应创建一个 Turn

  场景: 运行期间锁定任务管理
    假如 当前 Turn 正在运行
    当 用户查看新建、切换和移除任务操作
    那么 这些会破坏执行上下文的操作应具有可感知的禁用原因
    并且 当前任务与中断操作仍然可用

  场景: 移除最后一个任务后保持可输入
    假如 浏览器中只存在一个本地任务
    当 用户移除该任务
    那么 工作台应创建一个新的空任务
    并且 用户仍可访问任务输入器

  场景: 非终态事件流结束
    假如 Turn 事件流在 queued、running 或 waiting_approval 状态结束
    当 客户端没有收到任何终态事件
    那么 当前执行应进入公开连接失败状态
    并且 已收到的部分回答不应被持久化为成功回答

  场景: 中断请求失败
    假如 当前 Turn 正在运行
    当 用户请求中断但服务返回失败
    那么 当前执行应继续保留
    并且 工作台应显示不包含内部响应正文的中断失败提示

  场景: 检查器切换与关闭保持任务连续
    假如 用户已经输入尚未发送的任务草稿
    当 用户依次打开执行事件、切换 Turn 并关闭检查器
    那么 顶部活动视图和检查器标签应保持一致
    并且 输入草稿、当前任务与执行状态不应丢失

  场景: 长内容不破坏工作台布局
    假如 项目、模型、任务、事件和错误包含超长或多字节文本
    当 用户在 320 像素及以上视口查看工作台
    那么 文本应按区域截断、换行或滚动
    并且 页面不应产生横向溢出或遮挡主操作

  场景: 减少动态效果与背景资源降级
    假如 用户启用减少动态效果或主背景资源加载失败
    当 工作台显示空状态或执行状态
    那么 非必要等待与抽屉动画应停止
    并且 静态背景仍应保持正文、焦点和全部核心操作可用

  场景: 移动抽屉关闭后恢复任务焦点
    假如 用户在移动视口分别打开任务导航和执行检查器
    当 用户通过关闭按钮或遮罩退出抽屉
    那么 两个抽屉不应同时占据主画布
    并且 焦点应返回对应的打开控件
